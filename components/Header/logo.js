import { Box, Typography } from "@mui/material";

const Logo = () => {
  return (
    <Box

flex={1}>
      <Typography color={"whitesmoke"} fontSize={30} fontFamily={"BIXIERegular"}>خدماتي</Typography>
    </Box>
  );
};

export default Logo;
